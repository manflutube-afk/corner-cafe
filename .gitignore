node_modules/
.wrangler/
public/

__pycache__/

# Raw photo drops waiting to be processed into properly named, resized files.
# They stay on disk in images/ but are neither committed nor deployed.
images/IMG_*
